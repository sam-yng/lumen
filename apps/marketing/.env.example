# Public (client-safe) — inlined into the browser bundle.
# Canonical marketing-site origin. Used for metadataBase, robots, and sitemap.
NEXT_PUBLIC_SITE_URL=http://localhost:3001

# Where the "Open the app" / sign-in links point — the authenticated web app.
NEXT_PUBLIC_APP_URL=http://localhost:3000
