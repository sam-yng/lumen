{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "bunx turbo run build --filter=@lumen/web"
  },
  "deploy": {
    "startCommand": "bun run --filter @lumen/web start"
  }
}